def generate_next(current_value, index):
    if index % 2 == 0:
        next_value = current_value + index ** 2
    elif index % 2 == 1:
        next_value = current_value + int(index / 2)

def generate_next(current, index):
    if index % 3 == 0:
        next_value = current_value + index ** 2 + index
    elif index % 3 == 1:
        next_value = current_value - index
    else:
        next_value = current_value + index


def generate_next(current_value, index):
    if index % 2 == 0:
        next_value = current_value + index ** 3
    else:
        next_value = current_value + 2 * index ** 2 - index

def generate_next(current_value, index):
    if index % 3 == 0:
        next_value = current_value - index ** 2 + 3 * index
    elif index % 3 == 1:
        next_value = current_value + index ** 3
    else:
        next_value = current_value + 2 * index + 5

def generate_next(current_value, index):
    if index % 2 == 0:
        next_value = current_value + index ** 2 + index ** 3
    else:
        next_value = current_value - index + 3 * index ** 2

def generate_next(current_value, index):
    if index % 3 == 0:
        next_value = current_value + index ** 2 - index ** 3
    elif index % 3 == 1:
        next_value = current_value + 2 * index ** 2 + 5 * index
    else:
        next_value = current_value - 3 * index ** 3 + index
